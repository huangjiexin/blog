.home-blog .hero h1 {
    display: none !important;
}
.home-blog .hero .description {
    margin: 1.8rem auto;
    font-size: 1.6rem;
    line-height: 1.3;
    color: #fff;
    margin-top: 80% !important;
}
// 侧边栏样式
.sidebar ul.sidebar-links>li {
    background: #647ea0;
}

a.sidebar-link.active {
    font-weight: 600;
    color: #647ea0;
    background: var(--default-color-8);
    border-right: 3px solid #647ea0;
}

.page .sub-sidebar-wrapper li.level-3 {
    padding-left: 1.8rem;   
}

// 去除左上角logo
.navbar .logo {
    // display: none;
    border-radius: 10px;
}

// 右侧目录
.sub-sidebar-wrapper li a {
  padding: 0 1rem 0 0rem !important;
}
    